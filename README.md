# codori - **코**딩 테스트, **도**전 **리**팩토링!

엘리스트랙 SW 5기 1차 스터디 - 코딩테스트 문제풀이
프로그래머스 코딩 테스트 연습문제 0레벨 정복

소개 : 많은 코딩 테스트 문제를 해결하기 위해 노력함으로써 개발 실력을 향상시키고자 하는 스터디입니다.

문제 풀이 체크 : https://docs.google.com/spreadsheets/d/1NblZfQTC65UGY8qEkcIq0K0aSP37eD6NPgDOSXWaw6E/edit?hl=ko#gid=0

## 1주차

## 2주차

### 목표

- 각자 할당받은 프로그래머스 0레벨 10문제씩 풀이한다.

### 공부한 내용

- 원종석

  > 63 문자열 정수의 합 : spread 연산자를 사용하여 배열 변환, map, reduce 메소드 사용
  >
  > 67 배열에서 문자열 대소문자 변환하기 : 인덱스를 2로 나눈 나머지에 따라 toLowerCase, toUpperCase 사용
  >
  > 71 홀짝 구분하기 : input 값에 따라 짝수, 홀수 비교
  >
  > 75 주사위 게임 1 : 주사위의 숫자값에 따른 조건에 대하여 계산된 값 반환
  >
  > 79 배열 만들기 1 : spread 연산자를 사용하여 배열 초기화, map 메소드를 사용하여 값 할당
  >
  > 83 개미 군단 : 개미의 전투력을 정의하고, 큰 전투력부터 나누어 몫과 나머지를 구하여 병력 수에 할당
  >
  > 87 세균 증식 : 세균이 1시간에 두배만큼 증식하므로 처음 세균 마리수에 2를 n(시간)제곱하여 반환
  >
  > 91 꼬리 문자열 : filter 메소드를 이용하여 특정 문자열을 포함하지 않는 요소들만 반환 후 join 메소드를 이용하여 문자열로 반환
  >
  > 95 직각삼각형 출력하기 : input 값을 받아 별표 찍어서 직각삼각형 반환. repeat, join 메소드 사용
  >
  > 99 공백으로 구분하기 2 : trim 메소드를 사용하여 문자열의 양옆 공백 제거, split 메소드로 공백으로 나누어 배열 반환, filter 메소드를 사용하여 공백(길이가 0)을 제거한 배열 반환

- 김현규

  > 64 공백으로 구분하기 1 : split을 사용해 공백으로 구분함
  >
  > 68 n개 간격의 원소들 : for문을 사용해 i를 n으로 나눈 나머지가 0인 원소들만 차례로 배열에 담음
  >
  > 72 마지막 두 원소 : if문을 사용해 마지막 원소와 그전 원소의 값을 비교하여 연산 처리를 한 값을 배열에 담음
  >
  > 76 접두사인지 확인하기 : substring으로 주어진 문자열의 길이 만큼 자른 후 접두사인지를 판별함
  >
  > 80 문자열 바꿔서 찾기 : replaceAll을 사용해 문자열을 변경한 다음 주어진 문자열이 포함되는지 판단함
  >
  > 84 접미사인지 확인하기 : slice를 이용해 접미사의 길이만큼 뒤에서부터 잘라내고, 잘라낸 문자열이 접미사인지 판단함
  >
  > 88 가위 바위 보 : 문자열의 길이만큼 for문을 실행한 후, 가위바위보를 표현한 숫자를 이기는 경우로만 대입해 문자열을 더함
  >
  > 92 홀짝에 따라 다른 값 반환하기 : if 문을 사용하여 홀짝을 판단한 후 각자 주어진 연산을 함
  >
  > 96 n의 배수 고르기 : 주어진 배열을 for of를 사용해 반복하여 if문으로 n의 배수만 배열에 담
  >
  > 100 배열의 길이에 따라 다른 연산하기 : map을 사용하여 배열의 길이가 홀수면 모든 짝수에 n을 더하고, 짝수면 모든 홀수에 n을 더함

- 박상혁

  > 61 문자열 붙여서 출력하기 : 각 변수에 값을 할당하여 두 문자열을 이어서 출력.
  >
  > 65 문자열의 앞의 n글자 : slice 메소드의 범위를 인식하고, 종료 위치 n번째 문자 앞까지 잘라서 출력.
  >
  > 69 글자 이어 붙여 문자열 만들기 : map 메소드를 사용하여 리스트를 순회하며 필요한 문자만을 추출. join('')메소드를 사용하여 문자들을 이어붙였습니다.
  >
  > 73 문자열 곱하기 : repeat 메소드를 사용하여 반복하는 문자열을 출력.
  >
  > 77 문자열안에 문자열 : includes 메소드를 사용하여 특정 부분 문자열이 있는지 확인하고, 삼항 연산자를 사용하여 해당하는 결과를 출력
  >
  > 81 제곱수 판별하기 : sqrt 메소드를 사용하여 제곱근을 가진 수인지 확인하여 삼항 연산자를 사용하여 해당하는 결과를 출력
  >
  > 85 홀수 vs 짝수 : map 메소드의 index 요소를 사용하여 인덱스의 위치를 기준으로 짝수번째와 홀수번째의 합끼리 비교.
  >
  > 89 암호 해독 : for문에서 변수의 간격만큼 문자를 추출. 다른 사람의 풀이를 통해 filter 메소드를 사용하여 간결하게 풀이한 것도 확인 할 수 있었다.
  >
  > 93 주사위의 개수 : reduce 메소드를 사용하여 배열 요소에 대한 결과를 출력. Math.floor 함수로 소수점 이하를 버림. 문제 자체를 이해하지 못해 풀지 못할 뻔 하였다.
  >
  > 97 덧셈식 출력하기 :문자열 템플릿을 사용하여 결과 출력을 간결하게 할 수 있었다.

- 박민준
  > 62 수 조작하기 1 : switch ~ case 사용시 해당 조건문을 빠져나가는 break가 반드시 필요함
  >
  > 66 특정한 문자를 대문자로 바꾸기 : split, map , join을 사용하여 풀었는데 replaceAll()을 사용하면 더 간단하게 해결 가능
  >
  > 70 rny_string : (/찾을 문자열/g(전역에서 찾기), '바꾸고싶은 문자열')정규 표현식을 학습
  >
  > 74 원하는 문자열 찾기 : 문자열에서 문자열을 찾을땐 includes()를 활용
  >
  > 78 이어 붙인 수 : forEach를 활용할 때 매개변수에 (x,i) 중 x 는 각 인덱스의 원소, i 는 인덱스 위치
  >
  > 82 더 크게 합치기 : (``)백틱 기호를 사용하면 숫자를 문자처럼 붙힐 수 있다
  >
  > 86 A 강조하기 : map 메서드에서 조건식을 x === "a" || x === "A"를 사용하였는데 x.toLowerCase()를 사용해도 해당 원소가 소문자인지 확인가능
  >
  > 90 대문자와 소문자 : split과 join은 대부분 붙어 다니니 기억하기
  >
  > 94 최댓값 만들기 (2) : 배열에서 원소의 최대값을 구할때는 sort 메서드 사용
  >
  > 98 문자열 정렬하기 (1) : 메서드 체이닝으로 코드를 작성할 경우 한줄로 길게 쓰는것 보단 각 메서드별로 끊어서 한줄에 작성하는 것이 가독성을 높일 수 있다

### 회고

- git commit 규칙을 정하였다. 좋은 커밋 메시지를 남기는 것이 협업할 때 명확한 가이드라인을 제공할 수 있는 장점이 있어 미리 습관을 들여 연습하는 방법을 채택했다. 스터디에서는 단순 코드 커밋만 진행하기 때문에, 4가지 유형으로 커밋메시지 규칙을 정했다.
  - 처음 추가할때 : "add : n주차 문제 풀이 파일 추가 (이름)"
  - 수정사항이 생길 때 : "fix : 오타, 기능 등등... 수정"
  - README.md문서 수정 : "docs : 해당 문서 추가 수정.. "
  - 코드 리팩토링이나 다른방법으로 풀었을 때 : "refactor : 해당하는 기능"
- 스터디를 길게 진행하면 상당히 루즈해지기 때문에, 코드는 짧은 시간동안 보되 다른 사람이 풀었던 문제도 다시 풀면서 리팩토링하면 좋을 것 같다.
  - 풀이에 대한 의견 교환을 하며 startsWith/endsWith 함수처럼 처음 접하는 함수를 접할 수 있었습니다.
  - 다른 조원의 풀이를 들으면서 각자 선호하는 풀이방식과, 전에는 몰랐던 새로운 풀이방법(묵시적 형변환 등)을 접할 수 있었습니다.

## 3주차

### 공부한 내용

- 원종석

  > 103 인덱스 바꾸기 : 문자열을 배열로 변환하여 변수에 저장한 뒤, 입력받은 문자열을 charAt 메서드를 이용하여 해당 배열의 인덱스에 각각 배치하여 인덱스에 해당하는 문자 값을 서로 바꾼다. 그 뒤 join 메서드를 이용하여 문자열로 다시 변환한다.
  >
  > 107 외계행성의 나이 : 입력받은 나이 값을 숫자 배열로 변환하여 변수에 저장한다. 해당 값을 forEach 메서드를 이용, String.fromCharCode 메서드를 이용하여 배열의 숫자 값을 아스키코드값으로 변환하여 문자열에 더해준 뒤 값을 반환한다.
  >
  > 111 할 일 목록 : todo 배열에 finished 배열의 인덱스를 일치시켜 마치지 못한 요소만 filter 메서드를 이용하여 배열로 반환한다.
  >
  > 115 5명씩 : 5명씩 묶은 그룹의 가장 앞에 서있는 요소들만 배열로 반환하므로, 인덱스를 5로 나눈 나머지가 0인 요소만 filter 메서드를 이용하여 배열로 반환한다.
  >
  > 119 숫자 찾기 : 입력받은 정수를 배열로 변환한다. findIndex 메서드를 사용하여 해당 배열에 특정 정수가 있는지 찾아 해당 인덱스를 반환한다. 만약 없으면 -1을 반환한다. 문제에서는 자릿수를 반환해야 하기 때문에 인덱스가 존재하면 1을 더하여 반환한다.
  >
  > 123 배열 만들기 3 : 입력받은 2차원 배열을 구조 할당 분해를 사용하여 변수에 저장한다. 반환할 배열을 초기화하고, slice 메서드를 이용하여 정수 배열의 시작 인덱스부터 해당 인덱스까지의 요소들을 push한다. 마지막으로 flat 메서드를 이용하여 1차원 배열로 반환한다.
  >
  > 127 9로 나눈 나머지 : 숫자로 구성된 문자열을 배열 형태로 변환하고, reduce 메서드를 이용하여 배열의 모든 자릿수를 더한다. 그 뒤 9로 나눈 나머지를 반환한다.
  >
  > 131 모스부호 (1) : 문자열을 공백 기준으로 split 메서드를 이용하여 배열로 분리한다. morse 부호와 맞게 map 메서드를 이용하여 요소의 값들을 변환하고, 변환한 배열을 join 메서드를 이용하여 문자열로 반환한다.
  >
  > 135 팩토리얼 : fatorial 메서드를 구현하여 재귀 loop를 실행한다. 조건에 따라 가장 큰 정수를 반환한다.
  >
  > 139 가까운 수 : 절댓값 기준으로 가장 작은 순서로 정렬하고, 가까운 수가 2개라면 더 작은 수를 반환한다.

- 김현규

  > 104 배열 회전시키기 : pop,shift 를 사용해 배열의 처음,마지막 요소를 제거하고 그 요소를 각각 unshift,push로 처음,마지막에 추가함
  >
  > 108 피자 나눠 먹기 (2) : 피자의 수가 사람의 수로 나눴을때 나머지가 0일때까지 while문을 활용하여 반복해서 6을 더함
  >
  > 112 369 게임 : matchAll 메서드와, 정규 표현식을 사용해 3,6,9가 있는지를 판별함
  >
  > 116 문자열 섞기 : max를 사용해 두 문자열 중 더 긴 문자열을 판별하고 for문을 사용해 문자열의 요소를 하나씩 더함
  >
  > 120 ad 제거하기 : filter와 includes를 사용해 ad를 포함하지 않는 문자열만 배열에 담아서 반환함
  >
  > 124 문자열 정렬하기 (2) : toLowerCase로 문자열을 소문자로 변환하고, sort로 알파벳 순으로 정렬한 다음, join을 사용해 배열의 요소를 문자열로 합침
  >
  > 128 중복된 문자 제거 : set으로 중복된 문자열을 제거함
  >
  > 132 특별한 이차원 배열 2 : 이중 반복분을 사용해 이차원 배열을 하나씩 비교함
  >
  > 136 2차원으로 만들기 : slice로 n의 길이만큼 배열을 잘라내서 새로운 배열을 만들고 푸쉬함
  >
  > 140 등차수열의 특정한 항만 더하기 : for문을 사용해 included[i]가 true인 항들만 더함

- 박상혁

  > 101 가장 큰 수 찾기 : max 메소드로 배열 중에서 가장 큰 수를 찾고, 해당하는 자릿수를 for문을 통해 알아냈습니다. indexOf 메소드를 사용하면 자릿수를 더 쉽게 특정할 수 있습니다.
  >
  > 105 I로 만들기 : map메소드와 삼항연산자로 1보다 앞서는 모든 문자를 I로 바꾸어주었습니다. 그 후 join메소드를 사용하여 문자열로 결합하였습니다. replace함수와 정규표현식 //g를 통해 더 쉽게 출력할 수 있었습니다.
  >
  > 109 접미사 배열 : slice메소드를 통해 모든 접미사를 출력했습니다. sort메소드를 통해 사전순으로 나열 하였습니다.
  >
  > 113 약수 구하기 : for문을 통해 약수를 구했습니다. 초기값을 1로 두어 작은 수부터 오름차순으로 나열되도록 하였습니다.
  >
  > 117 주사위 게임 2 : and연산자와 or연산자를 사용하여 표현하였습니다. 제곱 계산이 필요하여 거듭제곱 연산자를 사용하였습니다. pow메소드로도 표현 가능합니다.
  >
  > 121 가까운 1 찾기 : 반복문의 출발점을 idx로 두어 1이 가장 가까운 자릿수를 찾기 쉽도록 하였습니다. indexOf로 더 쉽게 출력가능 합니다.
  >
  > 125 합성수 찾기 : 정수 중 최초의 합성수 4ㄹ를 초기값으로 설정하고, 약수를 찾을 때마다 카운트 하였습니다. 만일 매우 큰 수 였다면 2로 나눈값을 찾도록 하여 처리 시간을 단축하는것도 좋은 방법일 것이라고 의견을 나누었습니다.
  >
  > 129 두 수의 연산값 비교하기 : 두 수의 연산값을 각자 변수로 만들고 삼항연산자를 사용해 원하는 결과가 나오도록 하였습니다.
  >
  > 133 콜라츠 수열 만들기 : 콜라츠 수열의 조건을 만족시키는 while 문을 만들었습니다.
  >
  > 137 특별한 이차원 배열 1 : for반복문을 사용하여 결과를 구하고, 각 결과값들의 배열을 모아 최종 결과값을 출력하였습니다.

- 박민준
  > 102 부분 문자열 이어 붙여 문자열 만들기 : my_string에서 parts 인덱스에 해당하는 문자열을 추출하고, 추출된 문자열 순서대로 합쳤다.
  >
  > 106 배열 비교하기 : 각 배열의 길이를 비교하고, 배열의 길이가 다를때 각 배열에 있는 모든 원소의 합을 비교. 비교한 값으로 -1 or 1 or 0 반환한다.
  >
  > 110 0 떼기 : 문자열을 배열로 나누어 0이 있다면 결과에서 해당 인덱스를 제거한다.
  >
  > 114 순서 바꾸기 : n을 기준으로 끝까지 배열 자르기, 0부터 n까지 배열 자르기. 이 두 배열을 합쳐서 배열로 반환한다.
  >
  > 118 문자열 돌리기 : 입력받은 문자열을 split으로 자른 뒤 엔터 키를 기준으로 join하여 출력한다.
  >
  > 122 배열의 원소 삭제하기 : arr 배열에서 delete_list 배열에 해당하는 원소를 제외한 배열로 반환한다.
  >
  > 126 x 사이의 개수 : arr 배열에서 delete_list 배열에 해당하는 원소를 제외한 배열로 반환한다.
  >
  > 130 간단한 식 계산하기 : 구조 분해 할당을 통해 배열을 분해하고, 연산자에 따라 조건을 달리 하여 식을 계산하고, 값을 반환한다.
  >
  > 134 A로 B 만들기 : befor와 after를 배열로 나누어 정렬하고 합친 값이 동일한지 비교한다.
  >
  > 138 문자열 잘라서 정렬하기 : 문자 "x"를 기준으로 자른 후 공백은 제외하여 정렬한 배열을 반환한다.

### 회고

- 정답률 높은 문제부터 주차별로 풀이를 하기 때문에 3주차가 되면서 많이 고민해서 풀이해야 할 문제들이 많아졌다. 다음 주차에는 더 난이도 있는 문제가 있을 것인데, 최대한 많은 문제를 풀어서 의견을 공유해야겠다는 생각이 든다.
- 배열 관련 메서드를 이용할 때 여러 매개변수가 필요할 때가 있다. 인덱스 값만 사용할 경우 매개변수명을 정하지 않고 언더바를 사용하여 사용하지 않는 변수임을 정의하도록 해 보았다.

### 목표

-> 각자 할당받은 프로그래머스 0레벨 10문제씩 풀이한다.

## 4주차

### 목표

-> 각자 할당받은 프로그래머스 0레벨 10문제씩 풀이한다.

### 공부한 내용

- 원종석

  > 143 수열과 구간 쿼리 3 : 2차원 정수 배열을 forEach loop를 수행한다. 구조 분해 할당으로 서로의 위치를 바꾼 arr을 반환한다.
  >
  > 147 한 번만 등장한 문자 : 빈 객체를 선언한다. 문자열을 원소 1개씩 배열로 변환하여 객체에 문자와 그 갯수를 담는다. 이후 filter 메서드를 사용하여 문자의 갯수가 1인 데이터만 배열로 반환하고, 오름차순 정렬 후 join 메서드를 사용하여 문자열로 반환한다.
  >
  > 151 배열 만들기 5 : 문자열 배열의 원소들을 substr 메서드를 사용하여 잘라서 배열을 변환한다. 그 이후 filter 메서드를 사용하여 특정 값보다 큰 데이터만 배열로 반환한다.
  >
  > 155 글자 지우기 : 문자열을 배열로 변환 후 map 메서드를 사용하여 정수 배열에 해당하는 인덱스일 경우 빈 문자열, 아닐 경우 해당 문자 그대로 반환한다. 이후 join 메서드를 사용하여 다시 문자열로 변환 후 반환한다.
  >
  > 159 문자열이 몇 번 등장하는지 세기 : for loop를 수행한다. 문자열을 인덱스 위치부터 pat의 길이만큼 자르고, 자른 문자열이 pat과 일치한다면 횟수를 증가시킨다. loop가 종료되면 횟수를 반환한다.
  >
  > 163 세 개의 구분자 : 입력받은 문자열에서 a,b,c가 존재할 경우 replace와 정규표현식을 사용하여 해당 문자를 공백으로 변환한다. split 메서드를 사용하여 배열로 만든 후 filter 메서드를 사용하여 falsy한 값을 제거한다. (여기서는 빈 값) / 해당 배열의 길이가 0보다 크다면 그대로 배열을 반환하고, 아닐 경우 빈 배열이므로 EMPTY를 반환한다.
  >
  > 167 잘라서 배열로 저장하기 : for loop를 수행하여 my_str을 substr 메서드를 사용하여 잘라서 배열에 push한다. filter를 사용하여 falsy한 값을 제거 후 반환한다.
  >
  > 171 특수문자 출력하기 : ', " 는 앞에 \를 붙여 주어야 하므로 \를 붙여서 출력한다.
  >
  > 175 삼각형의 완성조건 (2) : 삼각형의 완성 조건에 따라 구간을 정하고, 갯수를 반환한다.
  >
  > 179 리스트 자르기 : 조건에 따라 다르게 배열을 잘라서 반환

- 김현규

  > 144 수 조작하기 2 : 반복문으로 switch문을 사용해 각 숫자에 대비되는 문자열을 return 한다.
  >
  > 148 수열과 구간 쿼리 1 : query의 요소 값을 배열에 담은 다음 반복문을 사용해 배열의 해당 인덱스 값을 증가시킨다.
  >
  > 152 세로 읽기 : 반복문에서 주어진 정수의 간격으로 증가시키면서 문자열을 더한다.
  >
  > 156 특정 문자열로 끝나는 가장 긴 부분 문자열 찾기 : lastIndexOf를 사용해 인덱스를 역순으로 탐색해서 최초로 마주치는 인덱스의 pat의 길이를 더해서 반환한다.
  >
  > 160 소인수분해 : while 문을 활용해서 n이 2이하가 될때까지 반복하고 n을 나눴을때 0이 되는 값이 소수이므로 그 값을 배열에 담아서 반환한다.
  >
  > 164 빈 배열에 추가, 삭제하기 : flag[i]가 true면, 배열을 2번추가해서 담고, false라면 원소를 제거한다.
  >
  > 168 문자열 계산하기 : 문자열을 split으로 나눈 뒤 for문으로 기호와 문자를 구분해서 계산한다.
  >
  > 172 문자열 묶기 : 새로운 배열을 선언하고 배열의 요소를 0으로 채운다음 해당하는 길이의 문자열이 나왔을때 1을 증가시키고 Math.max 메서드로 길이가 가장 긴 값을 return 한다.
  >
  > 176 커피 심부름 : 아메리카노면 4500을 더하고 카페라떼면 5000을 더하고, 아무거나를 적었을시엔 4500을 더해서 합을 반환한다.
  >
  > 180 조건 문자열 : if 문을 여러개 사용해 각 조건을 계산 한다음 반환한다.

- 박상혁

  > 141 k의 개수 : i부터 j까지 문자열로 나열하고 split으로 나누어 줌. for of루프로 digits의 요소를 하나씩 순회. 요소들 중 k와 동일한 숫자가 있다면 카운트하여 출력.
  >
  > 145 숨어있는 숫자의 덧셈 (2) : 대소문자 알파벳의 제거(toLowerCase, replace), 배열로 만들고 공백을 제거(split, filter), reduce메소드로 나누어진 배열을 합친 후 출력.
  >
  > 149 1로 만들기 : 배열의 5개 요소에 대한 출력값을 더하기 위해 forEach 메소드를 사용. 1이 아닐때, 연산을 할때마다 answer에 카운트하기 위해 while문을 사용.
  >
  > 153 문자열 뒤집기 : 뒤집을 문자의 범위를 for문으로 지정하고, 뒤집은 범위의 앞부분과 뒷부분도 구한 후 백틱(``)으로 합쳐서 표현.
  >
  > 157 이차원 배열 대각선 순회하기 : i(배열의 개수 확인), j(배열 안 요소의 개수 학인), i와 j를 합친 값 중에 k보다 작은 모든 수를 찾아 카운트한 후 출력.
  >
  > 161 7의 개수 : join과 split으로 배열을 나누고 filter 메소드로 7이 총 몇개인지 확인 후 length로 헤아려서 출력.
  >
  > 165 배열의 길이를 2의 거듭제곱으로 만들기 : n이 배열 길이보다 큰 2의 거듭제곱이 될 때까지 반복하기 위해 while문 사용. 마찬가지로 n이 그 길이에 도달할 때까지 push(0)을 해주기 위해 while문 사용.
  >
  > 169 수열과 구간 쿼리 4 : s~e까지의 수를 반복하는 i를 for문으로 적은 후 i에 해당하는 요소가 k의 배수라면 해당 i의 위치에 있는 요소에 +1을 하여 출력.
  >
  > 173 구슬을 나누는 경우의 수 : 배열 fact의 초기값을 fact[]로 설정해주고 Hint에 나온 경우의 값을 구하는 공식(n! / ((n-m)! * m!))에 맞춰서 적은 후 return.
  >
  > 177 qr code : 문자열 code를 배열로 바꾸고 reduce함수와 삼항연산자로 인덱스를 q로 나누었을 때 나머지가 r인 위치의 문자를 추가.

- 박민준
  > 142 진료순서 정하기 : 수가 클수록 낮은 숫자(환자의 응급도)를 위해 배열을 내림차순으로 정렬하여, 해당하는 index는 0부터 시작하기 떄문에 index의 위치 + 1로 낮은 숫자를 가진 배열을 반환한다.
  >
  > 146 문자열 반복해서 출력하기 : 문자열을 반복하기 위해 repeat 메서드를 사용하여 반복한다.
  >
  > 150 간단한 논리 연산 : AND와 OR 연산자를 사용할때 괄호를 통해 먼저 연산할 변수를 묶어야 한다.
  >
  > 154 이진수 더하기 : 이진수를 의미하는 문자열을 정수로 더해서 이진수로 반환할 때 parseInt(string, radix) 을 사용한다. 두번째 파라미터 radix(진수)는 2부터 36까지의 정수이다. JS는 string이 16진수나 8진수 표기법이 아니라면, radix를 10으로 설정하기 떄문에 생략하게되면 10으로 설정되어 계산된다. radix는 반드시 지정하여 혼동을 방지하는 것을 추천한다. 숫자를 2진수로 변환하기 위해서는 toString(n)를 사용하여 n진수로 변환한다.
  >
  > 158 컨트롤 제트 : 문자열을 split(" ")을 활용해 공백으로 나눈 배열을 반환하고, 배열에 원소가 숫자가 아닐때("Z")를 확인하여 앞에 있는 값은 빼고 나머진 전부 더한다.
  >
  > 162 공 던지기 : 공은 1번부터 던지며 오른쪽으로 한명을 건너뛰고 n+1의 사람이 받기 떄문에 주어진 k번째로 공을 던진 사람(배열의 원소의 값)은 2 \* (k-1) % numbers.length의 계산식이 나오게 된다.
  >
  > 166 영어가 싫어요 : "zero"~"nine"까지 배열을 설정하여 주어진 문자열에서 split 메서드를 활용해 설정한 배열로 나누어 index의 위치로 변환하여 join 메서드로 문자열 합침
  >
  > 170 조건에 맞게 수열 변환하기 2 : 배열을 비교할 때 JSON.stringify()를 활용하면 배열이 같은지 확인할 수 있다. 단순하게 비교 연산자를 활용하면 참조값을 비교하기 때문에 false이다.
  >
  > 174 2의 영역 : 배열에서 2의 모든 인덱스 위치를 확인하여 가장 큰값과 작은 값을 선언하고, 배열에 2가 포함되어있다면 가장 작은 인덱스부터 가장 큰인덱스 위치(포함) 까지 반환하기 위해 slice(작은값, 큰값+1)을 활용한다.
  >
  > 178 문자 개수 세기 : 배열을 만들때 배열을 리터럴 방식만이 아닌 Array 생성자 함수를 사용해서 만들 수 있다.

## 5주차

### 목표

-> 각자 할당받은 프로그래머스 0레벨 10문제씩 풀이한다.

### 공부한 내용

- 원종석
  > 183	두 수의 합 :
  >
  > 187	직사각형 넓이 구하기 :
  >
  > 191	왼쪽 오른쪽 :
  >
  > 195	정사각형으로 만들기 :
  >
  > 199	저주의 숫자 3 :
  >
  > 203	배열 만들기 6 :
  >
  > 207	배열 만들기 2 :
  >
  > 211	분수의 덧셈 :
  >
  > 215	겹치는 선분의 길이 : X
  >
  > 219	정수를 나선형으로 배치하기 :
  >

- 김현규
  > 184	배열 만들기 4 :
  >
  > 188	종이 자르기 :
  >
  > 192	치킨 쿠폰 :
  >
  > 196	무작위로 K개의 수 뽑기 :
  >
  > 200	특이한 정렬 :
  >
  > 204	전국 대회 선발 고사 :
  >
  > 208	코드 처리하기 :
  >
  > 212	다음에 올 숫자 :
  >
  > 216	주사위 게임 3 :
  >
  > 220	옹알이 (1) :
  >

- 박상혁
  > 181	외계어 사전 : spell 배열을 알파벳순으로 나열하고 문자열로 바꾸고 다른 매개변수도 map메소드를 통해 똑같이 문자열로 만들고, find메소드로 둘 사이에 일치하는게 있는가 확인 후 출력.
  >
  > 185	수열과 구간 쿼리 2 : filter메소드와 sort로 조건에 해당하는 요소들을 정렬. [0]을 사용해 그 중에서 가장 작은 인덱스 찾기. 삼항연산자 ?로 결과값 result가 존재한다면 그대로 push하고, 없다면 -1 출력.
  >
  > 189	문자열 겹쳐쓰기 : slice(0,s)로 0부터 s이전까지의 길이를 구하고 범위를 대체할 overwrite_string을 더하고, 마지막에 올 s와 overwrite_string 다음의 길이를 더해서 출력.
  >
  > 193	문자열 여러 번 뒤집기 : split으로 배열로 만ㄴ들고 forEach로 queries를 바꾸어줌. splice메소드로 제거해야하는 요소를 제거하고, 그 자리에 대체할 요소로 바꾸어줌.
  >
  > 197	유한소수 판별하기 : 조건에 맞추어 초기값을 구하고 2와 5로 마지막까지 나누어 1이 아닌 숫자로 끝난다면 2(무한소수)를, 1로 끝나면 1(유한소수)을 출력
  >
  > 201	그림 확대 : map 메소드로 기존 배열의 요소를 수정. repeat로 k 번만큼 반복(열 * k)하고, for문으로 k 번만큼 push(행 * k)하도록 함.
  >
  > 205	다항식 더하기 : '+'로 나누어 배열을 만들고 x가 있는 부분과 정수로 이루어진 부분을 별도로 더하여 결과값을 구함. if와 삼항연산자로 결과 출력.
  >
  > 209	문자열 출력하기 : console.log()를 사용하면 문자열을 출력할 수 있다.
  >
  > 213	연속된 수의 합 : x
  >
  > 217	배열 조각하기 : for문과 if문으로 조건에 맞게 만들고 결과를 출력.
  >

- 박민준
  > 182	a와 b 출력하기 : 
  >
  > 186	캐릭터의 좌표 :
  >
  > 190	로그인 성공? :
  >
  > 194	등수 매기기 :
  >
  > 198	대소문자 바꿔서 출력하기 :
  >
  > 202	문자열 밀기 :
  >
  > 206	최빈값 구하기 :
  >
  > 210	OX퀴즈 :
  >
  > 214	안전지대 : X
  >
  > 218	평행 : X
  >
